# Copy to `.env` for local runs. `.env` is git-ignored — never commit real tokens.
# Local Polari dev nodes require NO token (anonymous requests get full access),
# so for a local test you can leave POLARI_TOKEN unset entirely.

POLARI_BASE_URL=http://localhost:3000
# POLARI_TOKEN=
POLARI_TIMEOUT=15
