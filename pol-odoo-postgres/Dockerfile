# PostgreSQL for Odoo — the suite's FIRST postgres (Odoo requires it; it
# does NOT reuse MariaDB). Pinned tag: upgrades are their own receipted
# move (ODOO_INTEGRATION_PLAN.md risks), never a silent FROM bump.
FROM postgres:16.14

# Credentials arrive via env_file (pol-odoo-postgres/odoo-postgres.env,
# generated by setup-polari-security.sh). POSTGRES_PASSWORD is baked into
# the odoo-db-data volume at FIRST init — see pol-odoo/README.md for the
# credential-drift rescue procedure.
