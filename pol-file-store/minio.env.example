# MinIO root credentials — EXAMPLE (real file is gitignored + AUTOGENERATED)
# Generate with: ./setup-polari-security.sh dev --env-only
# Knobs: POLARI_MINIO_ROOT_USER, POLARI_MINIO_ROOT_PASS.
# Must stay in sync with client.env (the generator writes both together).
MINIO_ROOT_USER=polari-admin
MINIO_ROOT_PASSWORD=REPLACE_ME_RUN_setup-polari-security.sh
