# MinIO client credentials — EXAMPLE (real file is gitignored + AUTOGENERATED)
# Generate with: ./setup-polari-security.sh dev --env-only
# Values mirror minio.env (the generator writes both together).
MINIO_ACCESS_KEY=polari-admin
MINIO_SECRET_KEY=REPLACE_ME_RUN_setup-polari-security.sh
