# jinja-start
## SERVICE: psc-backend — annotated source (see suite-bundle.yml).
## Rendered per env by pol-build/manifests/suite-bundles.yml.
## variation: env — psc-backend differs per tier: dev | staging | prod
# {% if env_name == 'dev' %}
# 
#   # PSC Backend - Spring Boot Application
#   psc-backend:
#     build:
#       context: ./political-scorecard-node/political-scorecard-backend
#       dockerfile: Dockerfile.suite
#     container_name: psc-backend
#     image: psc-backend:{{ suite_image_tag }}
#     networks:
#       - polari-network
#     environment:
#       - SPRING_PROFILES_ACTIVE=dev
#       # Database connection to consolidated MariaDB
#       - SPRING_DATASOURCE_URL=jdbc:mariadb://pol-mariadb:3306/psc
#       - SPRING_DATASOURCE_USERNAME=psc-scorecard-server
#       # Resolved from suite-root .env (written by setup-polari-security.sh
#       # from pol-mariadb/mariadb.env); default matches legacy dev volumes.
#       - SPRING_DATASOURCE_PASSWORD=${PSC_DB_PASSWORD:-pscpassword}
#     env_file:
#       - ./pol-keycloak/keycloak-admin.env
#     volumes:
#       - ./political-scorecard-node/political-scorecard-backend:/app
#     command: ./startup_shell/startup_shell.sh
#     depends_on:
#       pol-mariadb:
#         condition: service_healthy
#       psc-redis:
#         condition: service_started
# {% elif env_name == 'staging' %}
# 
#   psc-backend:
#     build:
#       context: ./political-scorecard-node/political-scorecard-backend
#       dockerfile: Dockerfile.prod
#     container_name: psc-backend
#     image: psc-backend:{{ suite_image_tag }}
#     networks:
#       - polari-network
#     env_file:
#       - ./.generated/.env.{{ gen_env }}
#       - ./pol-keycloak/keycloak-admin.env
#       # MINIO_ACCESS_KEY / MINIO_SECRET_KEY — generated client credentials
#       # (mirrors pol-file-store/minio.env; setup-polari-security.sh owns both).
#       - ./pol-file-store/client.env
#     environment:
#       - SPRING_DATASOURCE_URL=jdbc:mariadb://pol-mariadb:3306/psc
#       - SPRING_DATASOURCE_USERNAME=psc-scorecard-server
#       # Resolved from --env-file .generated/.env.{{ gen_env }} (nip-staging-setup.sh
#       # syncs it from pol-mariadb/mariadb.env); default matches legacy volumes.
#       - SPRING_DATASOURCE_PASSWORD=${PSC_DB_PASSWORD:-pscpassword}
#       - SPRING_PROFILES_ACTIVE=prod
#       - JAVA_OPTS=-Xms128m -Xmx512m -XX:+UseContainerSupport -XX:MaxRAMPercentage=75.0
#       # MinIO connection (internal Docker network)
#       - MINIO_ENDPOINT=http://pol-file-store:9000
#       - MINIO_BUCKET=legislation
#     extra_hosts:
#       # Allow container to reach host machine via nip.io domains
#       # Uses ${BASE_DOMAIN} from --env-file .generated/.env.{{ gen_env }}
#       - "${BASE_DOMAIN}:host-gateway"
#       - "api.prf.${BASE_DOMAIN}:host-gateway"
#       - "api.psc.${BASE_DOMAIN}:host-gateway"
#       - "prf.${BASE_DOMAIN}:host-gateway"
#       - "psc.${BASE_DOMAIN}:host-gateway"
#       - "auth.${BASE_DOMAIN}:host-gateway"
#       - "files.${BASE_DOMAIN}:host-gateway"
#       - "s3.${BASE_DOMAIN}:host-gateway"
#     deploy:
#       resources:
#         limits:
#           memory: 768M
#           cpus: '0.5'
#         reservations:
#           memory: 256M
#           cpus: '0.1'
#     depends_on:
#       pol-mariadb:
#         condition: service_healthy
#       pol-file-store:
#         condition: service_healthy
#       psc-redis:
#         condition: service_started
# {% elif env_name == 'prod' %}
# 
#   psc-backend:
#     build:
#       context: ./political-scorecard-node/political-scorecard-backend
#       dockerfile: Dockerfile.prod
#     container_name: psc-backend
#     image: psc-backend:{{ suite_image_tag }}
#     networks:
#       - polari-network
#     env_file:
#       - ./.generated/.env.{{ gen_env }}
#       - ./pol-keycloak/keycloak-admin.env
#     environment:
#       - SPRING_DATASOURCE_URL=jdbc:mariadb://pol-mariadb:3306/psc
#       - SPRING_DATASOURCE_USERNAME=psc-scorecard-server
#       # Password comes from .env.prod via PSC_DB_PASSWORD
#       - SPRING_DATASOURCE_PASSWORD=${PSC_DB_PASSWORD:-pscpassword}
#       - SPRING_PROFILES_ACTIVE=prod
#       - JAVA_OPTS=-Xms128m -Xmx512m -XX:+UseContainerSupport -XX:MaxRAMPercentage=75.0
#       # MinIO connection (internal Docker network)
#       # Credentials come from .env.prod via MINIO_ACCESS_KEY / MINIO_SECRET_KEY
#       - MINIO_ENDPOINT=http://pol-file-store:9000
#       - MINIO_ACCESS_KEY=${MINIO_ACCESS_KEY:-polari-admin}
#       - MINIO_SECRET_KEY=${MINIO_SECRET_KEY:-polari-file-store-password}
#       - MINIO_BUCKET=legislation
#     deploy:
#       resources:
#         limits:
#           memory: 768M
#           cpus: '0.5'
#         reservations:
#           memory: 256M
#           cpus: '0.1'
#     depends_on:
#       pol-mariadb:
#         condition: service_healthy
#       pol-file-store:
#         condition: service_healthy
#       psc-redis:
#         condition: service_started
# {% endif %}
# jinja-end
