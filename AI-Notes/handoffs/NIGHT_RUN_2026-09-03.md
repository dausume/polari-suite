# Night run 2026-09-03 — meal planning, unattended

**His instruction (2026-09-03, late):** "work out the plan then go about
finishing the work on meal planning oriented stuff through the night as
we sleep." Everything lands on `dev` (committed per phase, NOT pushed —
his push ritual). This file is the running ledger; the last section is
the morning summary.

## Queue (in order; each row updated as it lands)

| # | Item | Source | State |
|---|---|---|---|
| P0 | VPN + federation PLAN (isle-mesh ↔ Polari grounded; no build) | his ask | ✅ AI-Notes/plans/VPN_FEDERATION_PLAN.md (vpn-0; his review gates vpn-1) |
| N1 | mo-1 mealoptions extraction → mo-2 PriceReference + source typing + local-first advice → mo-3 export path + privacy selftest | MEAL_OPTIONS_MODULE_PLAN.md | mo-1 ✅ (16 classes moved); mo-3 ✅ (POST /modules/export + `pol modules export`, hook protocol, privacy selftest 51/51, json_seeds 21/21); mo-2 ✅ (PriceReference: food × month × source type × chain-only-for-chains × coarse region; local-first advice knob 10 %; /prices/publish|references|advice; supply page panels; 33/33 + market 28/28) |
| N2 | **Today** page: a person's day in time order + "done" → WorkLedger (the owed auto-write) + the duration prompt | HOUSEHOLD_APP_PAGES §3.2, TESTING_OWED §6 | built 28/28 — wiring pending |
| N3 | **Shopping trip** page: purchase lines as a checklist by aisle order, prices in place → PriceObservation, bought → PantryItem lots | §3.3 | built 33/33 — wiring pending |
| N4 | **Cook now** page: the meal-prep event's steps with the person's minutes, safety notes, unattended-step timers, done → DurationObservation | §3.4 | built 33/33 — wiring pending |
| N5 | **Weekly review** page: planned vs eaten, cost vs budget, waste, fairness, consistency, next week's proposals | §3.6 | built 32/32 — wiring pending |
| N6 | Sugars column → "sweets" read directly (FDC subset carries sugars rows) | TESTING_OWED §8 | ✅ 24/49 foods cited (FDC 269/269.3); sweets basis = sugars-total else gl+carb, DGA 10 % ceiling labelled conservative; selftests 23+38+18+14 |
| N7 | Guests & situations: date-scoped situation → portions / purchase / packing | §3.8 | not started (time went to N8b fixes) |
| N8b | Fixes from the browser pass: form feedback survives refresh + `{today}` placeholders (fe), solutions return a readable `message` (be), date x-scale in plotFigure (fe), GeocoderDefinition prior `osm-nominatim-public` (seed), placeholders neutralised | N8 | ✅ fe: feedback service keyed display::item survives the refresh, `{today}`/`{now}`, date x-scale utc (main.f30f5819 rolled); be: every form solution returns a plain-words `message` (already-planned slots named and kept; dedupe cases say "kept") |
| W | Wiring: TodayAPI/ShoptripAPI/CookNowAPI/WeekReviewAPI in polariServer; SHOPTRIP classes+seeds; calendar_seed + mealplan_pages_seed pull the four page seed modules lazily (circular-import safe); nav Today / Cook now / Shopping trip / Weekly review; `pol topology assign mealoptions prf-a` | — | ✅ (lazy-imports 15/15, apps 45/45, pages 7/7) |
| N8 | Real-browser form clicks over CDP (apply-meal, log intake, geocode) | TESTING_OWED §7/§8 | ✅ real CDP clicks: Add-to-week creates a row (MealEntry 8→9, coverage 16→17), log intake + weight write rows + chart refetch, calendar list/week + event → CRUD dialog. FOUND: (1) form banner lost on refreshDisplay, (2) already-planned slot = silent no-op, (3) no GeocoderDefinition on staging, (4) static date placeholder, (5) Plot x-scale warnings → fixes queued as N8b |
| N8c | Post-fix real-browser (CDP) check | N8 | ✅ ALL PASS 06:20: already-planned slot → "Nothing added — 1 slot already planned and kept: Thu dinner (chicken-bowl-dinner)" survives the refresh, submit disabled in flight; log-weight blank date → "…on 2026-09-03…kept"; Mark done → "Marked … done; 12 min to the ledger" + WorkLedger row `ledger-mealprep-demo-alex-week-d3-breakfast`; geocode button → Nominatim 200, lat/lon + region filled; Plot date warnings 0. Recurring console error on every page: `GET /ai/voice` 404 (pre-existing) |
| N9 | Rebuild + roll + full headless sweep + commit to dev + morning summary | — | deploy 1 (05:24): APIs for all four pages + advice + publish + export answered; BUT `ensureDefinitionTables` crashed on a 2-tuple in SHOPTRIP_SEED_PAIRS (household shape is 3-tuples), so pages/nav/geocoder/typing seeds never ran → fixed (3-tuples + the three server seed loops now skip a malformed pair loudly) → deploy 2 ✅ 05:49: four pages render (no JSON), nav 4 items, geocoder prior, typing converged, publish 7 refs, export strips 9 fields; committed to dev (framework d7b25f8, angular a809334, cli c3adfa6, rf-node 15a2e75, suite fcc659b) — NOT pushed |

## Rules in force
No raw JSON on screens; embedded tables/graphs/calendars/maps and
api-structured-panel only; no new custom components. Every number a
labelled prior or cited. No diagnosis language. Person/place data never
enters mealoptions (see MEAL_OPTIONS_MODULE_PLAN.md §1). Names unchanged
on moves. Selftests may not be weakened.

## Morning summary

**Everything is on `dev`, clean, NOT pushed** (framework +37, angular
+12, polari-cli +10, rf-node +4, suite +36 commits ahead of origin).
Push = your ritual (`polari-cli/shells/push-all-dev.sh`); it will also
republish the module subtrees — `household` and `mealoptions` still have
`repo: ""` in the register, so their first publish is
`gh repo create polari-module-<name> --public` + `pol modules publish
<name>` (your call, outward-facing).

**Built tonight (all deployed on staging, verified live):**
1. VPN + federation PLAN (vpn-0): AI-Notes/plans/VPN_FEDERATION_PLAN.md —
   grounded in the isle (VLAN + agent containment), the exposure ledger,
   the one-peer WireGuard tunnel, PeerAgreement consent, the `.arch`
   rung. No code. The requester's message stays local (gitignored).
2. `modules/mealoptions/` — meal data split out of nutrition (16 classes,
   names unchanged, nutrition re-exports); PriceReference (food × month ×
   source type × chain-only-for-chains × coarse region; independents are
   a TYPE and say "varies by vendor"); local-first advice knob (10 %);
   `POST /modules/export` + `pol modules export` with the privacy hook;
   privacy selftest 51/51.
3. Four pages: Today (done → WorkLedger from both paths + the duration
   prompt), Shopping trip (aisle-ordered checklist → PriceObservation +
   pantry lot), Cook now (per-person minutes, safety lines, unattended
   timers, step done → DurationObservation + speed suggestion), Weekly
   review (planned vs eaten, cost vs budget, waste, fairness,
   consistency, next week's proposals + the Sunday trigger).
4. Sugars: 24/49 foods cited from FDC; "sweets" reads sugars-total when
   present, else GL+carb, and says which; DGA 10 % ceiling labelled
   conservative.
5. Browser-found fixes: form feedback now survives the refresh and shows
   the solution's message; `{today}`/`{now}` placeholders; date x-axes
   are real time axes; a geocoder definition prior; already-planned
   slots are named and kept instead of a silent no-op.

**Incident:** the first night deploy's boot seed pass crashed on a
2-tuple seed pair (my contract wording); fixed + the three server seed
loops now skip a malformed pair loudly. Deploy 2 clean.

**Browser-verified after the fixes (06:20):** feedback line survives the refresh, blank date = today, done → ledger row, geocode fills lat/lon via Nominatim, zero Plot date warnings.

**Not done:** N7 guests & situations; `pol modules publish` for the two
new modules; real store aisle rows. Owed list: TESTING_OWED §11.

