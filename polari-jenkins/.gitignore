# polari-jenkins — nothing that authenticates, nothing that is built, ever enters git
secrets/**
!secrets/README.md
!secrets/**/
!secrets/**/*.example
!secrets/**/.gitkeep
.env
device.env
pool/**
!pool/.gitkeep
jenkins_home/
*.token
*.gpg
*.asc
*.key
*.pem
*.pub
id_*
SETUP_STATUS.md
cosign.key
!cosign.pub
.controller-stamp
# dep-0: the deployment targets (ssh aliases + chosen names — never in git)
deploy/targets.env
